#create network for docker container 

#run app_backend with named volume and mind mount for backend folder also use unnamed volume for persist node_modulesS 
docker run 
 --network favorites-net 
 --name app_backend
 # named volumes for logs 
 -v logs:/app/logs
 #bind mount 
 -v /home/vasa/Documents/udemydocker/backend:/app
 #unnamed volume 
 -v /app/node_modules 
 --rm 
 -p 80:80 
 app-back

#run mongo db container with environment variables
docker run 
  --name mongodb  
  -v data:/data/db 
  -d 
  --rm 
  --network favorites-net 
  -e MONGODB_USERNAME=max mongo:4.0.8  

